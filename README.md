Commands to build the app:<br>
`cd cyberspeed-matrix-game`<br>
`mvn clean package`

Command to run the game:<br>
`java -jar ./target/scratch-game-exec.jar --config ../config.json --betting-amount 100`
